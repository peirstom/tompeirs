// Step 1: Import your component
import * as React from 'react'

// Defining the <Greeting> component
const Hero = (props) => {
    return (
      <p>Hi {props.name}!</p>
    )
  }

export default Hero